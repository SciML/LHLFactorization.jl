var documenterSearchIndex = {"docs":
[{"category":"section","location":"release_notes/#Release-Notes","page":"Release Notes","text":"","title":"Release Notes"},{"category":"section","location":"release_notes/#Unreleased","page":"Release Notes","text":"","title":"Unreleased"},{"category":"section","location":"release_notes/#Added","page":"Release Notes","text":"Sparse J (extension, loaded with SparseArrays and PureKLU): lhl(J::SparseMatrixCSC) solves the shifted family (σI + τJ)x = b for a sparse Jacobian. A Hessenberg similarity fills in an irreducible sparse block, so the only sparsity the reduction can exploit is reducibility: the extension takes the symmetric block triangular form (strongly connected components — the BTF of σI + τJ, with a symmetric permutation a similarity needs), reduces each irreducible diagonal block with this package's dense kernels or, where a per-block cost model finds it cheaper, a PureKLU sparse LU refactored per shift, and keeps the off-diagonal blocks sparse. The same verbs work on the returned factorization (lhl_shift!, lhl_ldiv!, lhl_refine!, lhl!, ldiv!, \\, a matrix right-hand side); shift = Complex{eltype(J)} gives complex shifts on a real reduction and thread = true threads the shift and reduction over blocks. It wins on block-triangular, dense-block or high-fill Jacobians and defers to a sparse LU elsewhere.\nAdjoint solves from the existing reduction: lhl_ldivH! solves Wᴴ x = b against the same reduction and the same shift LU as lhl_ldiv! (Wᴴ = Z⁻ᴴ Gᴴ Zᴴ — three O(n²) phases, no refactorization), with lhl_refineH! for iterative refinement against Aᴴ and applyZH!/applyZinvH! for the adjoint similarity transformations. A complex shift on a real reduction conjugates only the shifted half: Zᴴ = Zᵀ stays real.\nExplicit-vector kernels for fully complex workspaces (lhl(J::Matrix{ComplexF64}) and ComplexF32). The reduction's trailing update, trailing GEMM and panel GEMV now run real explicit-vector kernels on the interleaved storage, and the solves' Z sweeps run on real planes (a planar copy of the packed multipliers). Measured on one Zen2 core, a ComplexF64 reduction runs at 2–3.8× the same-size real one (the flop ratio is 4) instead of 6.4–7.5×, and lhl_ldiv! at ≈2.5× instead of ≈4.7×. The threaded reduction now covers ComplexF64 (n ≥ 512) and ComplexF32 (n ≥ 1024), and is still bit-identical for any thread count. The factors layout is unchanged.\nComplex pivot magnitudes and balance norms are now |re| + |im| (LAPACK's CABS1, as the shifted LU already used), replacing abs. Pivot choices of a complex reduction can therefore differ from v2.0.0 in near-ties; results differ at rounding level, both choices are equally valid partial pivoting, and complex multipliers are now bounded by √2 in modulus (LAPACK's own bound for zgetrf) rather than 1. Real workspaces are bit-for-bit unaffected.","title":"Added"},{"category":"section","location":"release_notes/#Fixed","page":"Release Notes","text":"lhl_ldivH! (and the applyZinvH! it drives) gave wrong Float64 adjoint solves on 128-bit-SIMD targets (e.g. AArch64/NEON, where a Float64 vector holds two lanes). The packed back-substitution _lhl_zinvsweepH_buf! is pipelined: it issues the next group's body dot products before the current group's head is resolved, then folds the four intra-group coupling rows back in from registers. That fold treated the four rows as the first SIMD vector, which is only true when a vector spans at least four lanes; with two lanes the four rows straddle two vectors, so the second was counted twice and the solve was off by O(1). The forward solve, applyZH!, the adjoint Hessenberg solve, and every Float32 path (four lanes even at 128 bits) were already correct, as was every 256-bit target. The kernel now falls back to the width-agnostic generic sweep when a vector holds fewer than four lanes, and stays non-allocating. This landed before any release contained the adjoint API.","title":"Fixed"},{"category":"section","location":"release_notes/#v2.0","page":"Release Notes","text":"Breaking. The changes below are mechanical to adopt; lhl, lhl!, lhl_reduce!, applyZ!/applyZinv! and the LHLWorkspace{T}(n) constructor are all source compatible with v1.","title":"v2.0"},{"category":"section","location":"release_notes/#Breaking","page":"Release Notes","text":"LHLWorkspace gained a third type parameter, LHLWorkspace{T, Tr} → LHLWorkspace{T, Tr, TG}, where TG is the element type the shifted Hessenberg is held in. Only code that writes the parameters out is affected: an annotation ws::LHLWorkspace{Float64, Float64} must become LHLWorkspace{Float64, Float64, Float64} or simply ws::LHLWorkspace. The constructor LHLWorkspace{T}(n) is unchanged, so most code needs no edit at all.\nlhl_ldiv! now throws DimensionMismatch when x's length does not match the workspace. v1 read past the end and returned a wrong answer silently, so anything this now catches was already broken.\nThe shift moved out of the workspace into LHLShift. The shift-taking methods gain a shift argument — lhl_shift!(sh, ws, σ, τ), lhl_ldiv!(x, sh, ws), lhl_refine!(x, A, b, sh, ws, steps). The one-shift forms lhl_shift!(ws, σ, τ), lhl_ldiv!(x, ws) and lhl_refine!(x, A, b, ws, steps) still work and operate on the workspace's own shift, ws.shift; ws.σ, ws.τ and ws.info still read and write through to it. Code using one shift per reduction needs no change.","title":"Breaking"},{"category":"section","location":"release_notes/#Added-2","page":"Release Notes","text":"LHLShift{TG} — a shift is now a first-class object, so one reduction can serve several shifts. Build extra ones with LHLShift{TG}(ws).\nComplex shifts on a real reduction: lhl(J; shift = ComplexF64), or LHLWorkspace{T}(n; shift = TG). A real J keeps a real O(n³) reduction while the shift alone goes complex — the shape RadauIIA needs, where a real and a complex stage matrix come from one Jacobian.\nThreading: lhl(J; thread = Val(true)) (the default) and lhl_reduce!(ws, J, balance, thread) run the blocked reduction on Polyester threads when Polyester is loaded and Threads.nthreads() > 1. Deterministic: the result is bit-identical for any thread count. Pass Val(false) to disable.","title":"Added"},{"category":"section","location":"release_notes/#Performance","page":"Release Notes","text":"Rewritten kernels. Measured against LAPACK on one thread, a re-shift is 14× cheaper than an LU refactorization at n = 25 and 108× at n = 800, and lhl_ldiv! is now about the cost of an LU's triangular solves rather than twice it.","title":"Performance"},{"category":"section","location":"release_notes/#Notes","page":"Release Notes","text":"One workspace, and one LHLShift, cannot serve concurrent solves — both write scratch buffers. Give each thread its own.","title":"Notes"},{"category":"section","location":"api/#API-Reference","page":"API","text":"","title":"API Reference"},{"category":"section","location":"api/#Module","page":"API","text":"","title":"Module"},{"category":"section","location":"api/#Workspaces","page":"API","text":"","title":"Workspaces"},{"category":"section","location":"api/#Reduction","page":"API","text":"","title":"Reduction"},{"category":"section","location":"api/#Shifts-and-solves","page":"API","text":"","title":"Shifts and solves"},{"category":"section","location":"api/#Adjoint-solves","page":"API","text":"","title":"Adjoint solves"},{"category":"section","location":"api/#Similarity-transformations","page":"API","text":"","title":"Similarity transformations"},{"category":"section","location":"api/#Sparse-routing","page":"API","text":"Predicates a consumer (for example a linear-solver stack) can use to decide when the sparse J path — the SparseArrays + PureKLU extension — is worth taking.","title":"Sparse routing"},{"category":"module","location":"api/#LHLFactorization","page":"API","text":"LHLFactorization\n\nThe LHL factorization: a reduction of a square matrix to upper Hessenberg form by Gaussian similarity transformations with partial pivoting,\n\nJ = Z H Z⁻¹,   Z = D·P·L\n\nwith L unit lower triangular (multipliers bounded by 1 in modulus — by √2 for a complex J, whose pivot magnitudes are |re| + |im|, as LAPACK's), P a permutation and D a balancing diagonal.  This is Wilkinson's elimination method — EISPACK's ELMHES — packaged for a purpose it is rarely exposed for: solving a family of shifted systems\n\n(σI + τJ) x = b\n\nfor many (σ, τ) against one reduction.  The shift never reaches Z, so\n\nσI + τJ = Z (σI + τH) Z⁻¹\n\nand σI + τH is Hessenberg: a new shift costs O(n²), against O(n³) for a fresh LU. The motivating case is the iteration matrix W = I - γJ of a stiff ODE solver, where adaptive step-size control changes γ every step while J is held fixed for tens of them. A real J may take complex shifts — resolvents (sI - J)⁻¹, the complex stage systems of Radau methods — while its reduction stays real: lhl(J; shift = ComplexF64), or an LHLShift held next to the workspace.\n\nExample\n\nusing LHLFactorization, LinearAlgebra\n\nJ = randn(400, 400)\nws = lhl(J)                      # O(n³), once\nfor γ in (0.01, 0.013, 0.02)\n    lhl_shift!(ws, 1, -γ)        # O(n²), per shift: builds I - γJ\n    x = lhl_ldiv!(copy(b), ws)   # O(n²), per right-hand side\nend\n\nTo drive this from a linear-solver interface rather than by hand, LinearSolve.jl's LHLFactorization wraps it, and consumes SciMLOperators.WOperator — the split J - M/γ an implicit ODE solver already builds — as its system matrix.\n\nStability\n\nZ is not orthogonal, so the backward error carries a factor κ(Z) an LU does not have. Partial pivoting is not optional (without it the method loses every digit on ordinary matrices), and one step of iterative refinement — see lhl_refine! — restores a backward error comparable to LU's even on the near-nilpotent matrices where κ(Z) reaches 10¹⁰.\n\n\n\n\n\n","title":"LHLFactorization"},{"category":"type","location":"api/#LHLFactorization.LHLWorkspace","page":"API","text":"LHLWorkspace{T}(n; shift = T)\n\nStorage for one LHL factorization: the reduction of J (element type T) and, in ws.shift::LHLShift{shift}, the LU of the current shifted Hessenberg.  Build one with lhl or lhl!.  shift = Complex{T} on a real T gives a real reduction whose shifts and solves are complex — see LHLShift; T and Complex{real(T)} are the two choices.  (LHLWorkspace{T, Tr, TG}: Tr = real(T), TG the shift type.)\n\nfactors holds H in triu(factors, -1) and the step-k multipliers in the annihilated positions factors[k+2:n, k], exactly the way an LU packs its own; Lp holds the same multipliers repacked for the solves (see _lhl_lpack!: groups of four columns interleaved in W-row tiles, zero padded, so that a rank-4 sweep reads one contiguous stream with no remainder loop); for a ComplexF32/ComplexF64 workspace Lpp holds them once more as two real planes, which the solves' planar sweeps read instead.  perm/iperm are the pivot permutation P and its inverse, iscale the reciprocals of the balancing scale (powers of two, so exact), and xbuf a padded copy of the vector applyZ!/applyZinv! work in.  Ht holds H transposed (see LHLShift for why); Ht, work and pack are scratch during the reduction, which fills Ht last.  factors is an n×n view into fstore, whose leading dimension is padded so that no small multiple of the column stride falls within a vector of a multiple of 4 KiB (the reduction's row-block sweep would otherwise stall on loads that alias its own stores a few columns back).\n\nThe fields of ws.shift (Gt, rdiag, swap, resid, σ, τ, info) are also reachable as properties of ws.  The solves write xbuf (and lhl_refine! resid), so one workspace must not serve concurrent solves; give each thread its own.\n\n\n\n\n\n","title":"LHLFactorization.LHLWorkspace"},{"category":"type","location":"api/#LHLFactorization.LHLShift","page":"API","text":"LHLShift{TG}(n)\nLHLShift{TG}(ws::LHLWorkspace)\n\nThe shift-dependent half of an LHL solve: the LU of one σI + τH and the buffers its solves need, with element type TG.  Every LHLWorkspace owns one (ws.shift), which the two-argument lhl_shift!(ws, σ, τ) / lhl_ldiv!(x, ws) use; build more to hold several shifts against one reduction at once — a real one and a complex one on the same real J, as a Radau-type implicit Runge–Kutta step needs — and pass them explicitly: lhl_shift!(sh, ws, σ, τ), lhl_ldiv!(x, sh, ws).  TG is the reduction's element type T or Complex{T}.\n\nGt holds the LU of σI + τH transposed: the Hessenberg elimination sweeps rows, and in a column-major array the transposed layout turns every inner loop of the per-shift work (fused rebuild and elimination, back substitution) into a contiguous one; it has extra zero rows below row n so the back substitution's dot products run to a full vector.  For a complex TG the storage is real and planar: each column of Gt holds the real parts in rows 1:n and the imaginary parts in rows po+1:po+n (po the plane offset), and work and xbuf are laid out the same way, so that every inner loop is a real one — the Z sweeps run the real multipliers over each plane, and the complex products of the elimination and the back substitution are four real multiply–adds with no lane shuffling.  rdiag holds the reciprocals of the pivots, swap the interchanges, info the index of the first zero pivot (0 if none), σ/τ the shift loaded.  work is the pending row of lhl_shift!, resid the residual of lhl_refine!, xbuf the padded copy of the right-hand side the solves work in — so one LHLShift must not serve concurrent solves.\n\n\n\n\n\n","title":"LHLFactorization.LHLShift"},{"category":"function","location":"api/#LHLFactorization.lhl","page":"API","text":"lhl(J; balance = true, shift = eltype(J), thread = Val(true)) -> LHLWorkspace\n\nReduce J to upper Hessenberg form by Gaussian similarity with partial pivoting.  J is not modified; lhl! reuses an existing workspace instead of allocating one. shift is the element type of the shifts and solves; Complex{eltype(J)} on a real J keeps the reduction real and makes only the shifted half complex (see LHLShift).\n\nthread = Val(true) lets the blocked reduction (n ≥ 500 for Float64, 512 for ComplexF64, 1024 for Float32 and ComplexF32; other element types stay serial) run on Polyester threads; threading requires using Polyester (which loads the LHLFactorizationPolyesterExt extension) and julia -t N — without either, Val(true) silently runs the serial code.  Val(false) (or false) keeps it single-threaded.  The threaded work is partitioned independently of the thread count, so factors, Lp, Ht and the solves are bit-identical with and without threads.  The shifts and solves are serial and work per right-hand side; nothing here reads or sets the BLAS thread count.\n\nFollow with lhl_shift! to load a shift and lhl_ldiv! to solve.\n\n\n\n\n\n","title":"LHLFactorization.lhl"},{"category":"function","location":"api/#LHLFactorization.lhl!","page":"API","text":"lhl!(ws, J; balance = true, thread = Val(true)) -> ws\n\nReduce J into the existing workspace ws, resizing it if needed, and return ws.  J is not modified.  The in-place counterpart of lhl, which documents balance and thread; the shift element type is fixed when ws is built and is not a keyword here.\n\n\n\n\n\n","title":"LHLFactorization.lhl!"},{"category":"function","location":"api/#LHLFactorization.lhl_reduce!","page":"API","text":"lhl_reduce!(ws, J, balance, thread = Val(true)) -> ws\n\nReduce J to upper Hessenberg form by Gaussian similarity transformations with partial pivoting, into ws.  J is not modified.  thread (Val(true)/Val(false) or a Bool) allows the reduction to run on Polyester threads when Polyester is loaded; see lhl.\n\n\n\n\n\n","title":"LHLFactorization.lhl_reduce!"},{"category":"function","location":"api/#LHLFactorization.lhl_shift!","page":"API","text":"lhl_shift!(ws, σ, τ) -> ws\nlhl_shift!(sh::LHLShift, ws, σ, τ) -> sh\n\nForm G = σI + τH and LU-factorize it (partial pivoting) into ws.shift — or into the separately held sh, resized to ws if needed — transposed, in Gt.  ≈n² multiply–adds.  (σ, τ) = (1, -γ) gives I - γJ; (0, 1) gives J itself.  The shift is converted to the LHLShift's element type: a complex σ or τ needs one built for complex shifts (LHLWorkspace{T}(n; shift = Complex{T}), lhl(J; shift = Complex{T}) or LHLShift{Complex{T}}(ws)), and throws an ArgumentError otherwise.  A zero pivot is reported in info (the index of the first), not thrown.\n\nAn upper Hessenberg matrix has one subdiagonal, so each elimination step chooses between two candidates and the element growth factor is bounded by n — far tighter than the 2ⁿ⁻¹ of general partial pivoting.\n\n\n\n\n\n","title":"LHLFactorization.lhl_shift!"},{"category":"function","location":"api/#LHLFactorization.lhl_ldiv!","page":"API","text":"lhl_ldiv!(x, ws)\nlhl_ldiv!(x, sh::LHLShift, ws)\n\nx ← W⁻¹x for the W currently loaded by lhl_shift! into ws.shift (or into sh): Z⁻¹, Hessenberg solve, Z.  3n²/2 multiply–adds.  x must be able to hold the shift's element type (a real x cannot take a complex shift).  Uses the shift's xbuf as scratch, so concurrent solves must each have their own.\n\n\n\n\n\n","title":"LHLFactorization.lhl_ldiv!"},{"category":"function","location":"api/#LHLFactorization.lhl_refine!","page":"API","text":"lhl_refine!(x, A, b, ws, steps)\nlhl_refine!(x, A, b, sh::LHLShift, ws, steps)\n\nApply steps rounds of fixed-precision iterative refinement to a solve of A x = b, where ws (with ws.shift, or with sh) holds the factorization of A.  Z is not orthogonal, so the raw solve's backward error carries a factor κ(Z); refinement buys it back for the price of a matvec and a second O(n²) solve (Skeel 1980).  One step is enough to match LU's backward error even on matrices where κ(Z) reaches 10¹⁰.\n\n\n\n\n\n","title":"LHLFactorization.lhl_refine!"},{"category":"function","location":"api/#LHLFactorization.lhl_ldivH!","page":"API","text":"lhl_ldivH!(x, ws)\nlhl_ldivH!(x, sh::LHLShift, ws)\n\nx ← W⁻ᴴx, the adjoint counterpart of lhl_ldiv!, against the same reduction and the same shift LU: W = Z G Z⁻¹ gives Wᴴ = Z⁻ᴴ Gᴴ Zᴴ, so the solve is Zᴴ, an adjoint Hessenberg solve on the stored factors, then Z⁻ᴴ — three O(n²) phases and no new factorization.  x must be able to hold the shift's element type, and a complex shift on a real reduction conjugates only the shifted half (Zᴴ = Zᵀ is real). Uses the shift's xbuf as scratch, so concurrent solves must each have their own.\n\n\n\n\n\n","title":"LHLFactorization.lhl_ldivH!"},{"category":"function","location":"api/#LHLFactorization.lhl_refineH!","page":"API","text":"lhl_refineH!(x, A, b, ws, steps)\nlhl_refineH!(x, A, b, sh::LHLShift, ws, steps)\n\nApply steps rounds of fixed-precision iterative refinement to a solve of Aᴴ x = b — the adjoint counterpart of lhl_refine!, driven by lhl_ldivH!.  A is the matrix the workspace factorized, not its adjoint: the residual is formed with mul!(r, A', x), so A only needs a lazy adjoint matvec.\n\n\n\n\n\n","title":"LHLFactorization.lhl_refineH!"},{"category":"function","location":"api/#LHLFactorization.applyZ!","page":"API","text":"applyZ!(x, ws)\n\nx ← Z x.  n²/2 multiply–adds.  Uses ws.xbuf (for a complex workspace, the shift's planar buffer) as scratch, so not thread-safe on a shared workspace.\n\n\n\n\n\n","title":"LHLFactorization.applyZ!"},{"category":"function","location":"api/#LHLFactorization.applyZinv!","page":"API","text":"applyZinv!(x, ws)\n\nx ← Z⁻¹x.  n²/2 multiply–adds.  Uses ws.xbuf (for a complex workspace, the shift's planar buffer) as scratch, so not thread-safe on a shared workspace.\n\n\n\n\n\n","title":"LHLFactorization.applyZinv!"},{"category":"function","location":"api/#LHLFactorization.applyZH!","page":"API","text":"applyZH!(x, ws)\n\nx ← Zᴴ x.  n²/2 multiply–adds against the same packed multipliers as applyZ!: Z = D·P·L gives Zᴴ = Lᴴ Pᵀ D (D is real even for a complex reduction).  Uses ws.xbuf as scratch, so not thread-safe on a shared workspace.\n\n\n\n\n\n","title":"LHLFactorization.applyZH!"},{"category":"function","location":"api/#LHLFactorization.applyZinvH!","page":"API","text":"applyZinvH!(x, ws)\n\nx ← Z⁻ᴴ x.  n²/2 multiply–adds; Z⁻ᴴ = D⁻¹ P L⁻ᴴ, the adjoint counterpart of applyZinv!.  Uses ws.xbuf as scratch, so not thread-safe on a shared workspace.\n\n\n\n\n\n","title":"LHLFactorization.applyZinvH!"},{"category":"function","location":"api/#LHLFactorization.lhl_isreduced","page":"API","text":"lhl_isreduced(F) -> Bool\n\nWhether the factorization F (an LHLWorkspace, or the sparse factorization the SparseArrays/PureKLU extension returns) currently holds a reduction.  A consumer that caches a reduction across shifts — LinearSolve.jl's LHLFactorization — asks this to decide whether it must re-reduce.\n\n\n\n\n\n","title":"LHLFactorization.lhl_isreduced"},{"category":"function","location":"api/#LHLFactorization.lhl_prefers_sparse","page":"API","text":"lhl_prefers_sparse(J::SparseMatrixCSC; lhl_max = 4096) -> Bool\n\nA cheap, structure-only gate for choosing the sparse LHL solver over a sparse LU on J: it is worth reducing when J is reducible — its symmetric block triangular form has more than one block and its largest irreducible block is at most lhl_max — since that is the regime where the per-block reduction can beat a sparse LU (one big irreducible block is KLU's).  The per-block kernel = :auto cost model then makes the fine choice at factorization time.  A method is provided by the sparse extension; the fallback is false.\n\nLinearSolve.jl's defaultalg uses this to decide whether a sparse-J WOperator should default to LHLFactorization.\n\n\n\n\n\n","title":"LHLFactorization.lhl_prefers_sparse"},{"category":"section","location":"#LHLFactorization.jl","page":"Home","text":"LHLFactorization reduces a dense square matrix J to upper Hessenberg form once and reuses that reduction for a whole family of shifted systems (σI + τJ) x = b. The reduction is O(n³) and happens once; loading a new shift and solving a right-hand side are both O(n²). That is the trade an implicit ODE solver wants, where γ moves every step while J is held for tens of steps.","title":"LHLFactorization.jl"},{"category":"section","location":"#Solve-shifted-systems","page":"Home","text":"using LHLFactorization, LinearAlgebra\n\nJ = randn(40, 40)\nb = randn(40)\nws = lhl(J)                        # O(n³), once\n\nfor γ in (0.01, 0.02, 0.04)\n    lhl_shift!(ws, 1.0, -γ)        # O(n²), per shift: builds I - γJ\n    x = lhl_ldiv!(copy(b), ws)     # O(n²), per right-hand side\n    @assert (I - γ * J) * x ≈ b\nend\n\nA workspace is reused for a new matrix with lhl! and for many right-hand sides with lhl_ldiv!. lhl_reduce! is the non-keyword reduction entry point, and applyZ! / applyZinv! apply the similarity transformation itself.","title":"Solve shifted systems"},{"category":"section","location":"#Backward-error-and-refinement","page":"Home","text":"The similarity Z is not orthogonal, so the raw solve's backward error carries a factor κ(Z) that an LU does not have. One step of lhl_refine! buys it back for the price of a matvec and a second O(n²) solve.\n\nγ = 0.05\nA = I - γ * J\nlhl_shift!(ws, 1.0, -γ)\nx = lhl_ldiv!(copy(b), ws)\nlhl_refine!(x, A, b, ws, 1)\n@assert A * x ≈ b","title":"Backward error and refinement"},{"category":"section","location":"#Adjoint-solves","page":"Home","text":"The reduction already contains everything an adjoint solve needs: W = Z G Z⁻¹ gives Wᴴ = Z⁻ᴴ Gᴴ Zᴴ, so lhl_ldivH! solves Wᴴ x = b in the same three O(n²) phases against the same reduction and the same shift LU — nothing is refactorized. That is what a reverse-mode sweep through a stiff solve wants on its backward pass. lhl_refineH! refines it (the residual is formed with a lazy A'), and applyZH! / applyZinvH! apply the adjoint similarity on its own. On a real reduction Zᴴ = Zᵀ stays real even under a complex shift; only the shifted half conjugates.\n\nlhl_shift!(ws, 1.0, -γ)\nxh = lhl_ldivH!(copy(b), ws)\nlhl_refineH!(xh, A, b, ws, 1)\n@assert A' * xh ≈ b","title":"Adjoint solves"},{"category":"section","location":"#Complex-shifts-on-a-real-reduction","page":"Home","text":"A Radau-type implicit Runge–Kutta step needs one real and one complex shift against the same real Jacobian. Passing shift = Complex{T} keeps the O(n³) reduction real and makes only the O(n²) shifted half complex. An LHLShift is the shift-dependent half on its own, so several shifts can be held against one reduction at the same time and passed explicitly to the three-argument lhl_shift! and lhl_ldiv!.\n\nws = lhl(J; shift = ComplexF64)        # real reduction, complex shifts\nlhl_shift!(ws, 1.0, -0.05 + 0.02im)\nz = lhl_ldiv!(ComplexF64.(b), ws)\n@assert (I - (0.05 - 0.02im) * J) * z ≈ b\n\nwr = lhl(J)                            # a real workspace ...\nsh = LHLShift{ComplexF64}(wr)          # ... with an extra complex shift alongside it\nlhl_shift!(wr, 1.0, -0.05)             # real shift, into wr.shift\nlhl_shift!(sh, wr, 1.0, -0.05 + 0.02im)   # complex shift, into sh\nxr = lhl_ldiv!(copy(b), wr)\nxc = lhl_ldiv!(ComplexF64.(b), sh, wr)\n@assert (I - 0.05 * J) * xr ≈ b\n@assert (I - (0.05 - 0.02im) * J) * xc ≈ b","title":"Complex shifts on a real reduction"},{"category":"section","location":"#Threading","page":"Home","text":"The blocked reduction (n ≥ 500 for Float64, n ≥ 512 for ComplexF64, n ≥ 1024 for Float32 and ComplexF32; other element types stay serial) runs on Polyester threads when thread = Val(true) — the default — and Polyester is loaded. Polyester is a weak dependency: using Polyester loads the LHLFactorizationPolyesterExt extension, and without it (or without julia -t N) Val(true) silently runs the serial code. Val(false) keeps it single-threaded.\n\nusing LHLFactorization, Polyester       # loads LHLFactorizationPolyesterExt\n\nJ = randn(600, 600)\nws = lhl(J; thread = Val(true))         # threaded when julia runs with -t N\nserial = lhl(J; thread = Val(false))\n@assert ws.factors == serial.factors    # bit-identical, whatever the thread count\n\nThe threaded reduction partitions its work independently of the thread count, so factors, Lp, Ht and every solve are bit-identical with and without threads. Shifts and solves are serial and work per right-hand side; nothing in the package reads or sets the BLAS thread count. A workspace uses its own scratch buffers during a solve, so concurrent solves need one workspace each.\n\nSee the API reference for the complete workspace lifecycle.","title":"Threading"}]
}

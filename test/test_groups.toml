# Core = the top-level test/*.jl files: core_tests.jl and threading.jl (the latter runs
# its testset twice, with and without the Polyester extension loaded).
[Core]
versions = ["lts", "1", "pre"]

["Core 32-bit"]
group = "Core"
versions = ["1"]
os = ["ubuntu-latest"]
arch = "x86"

[Interface]
versions = ["lts", "1"]

[QA]
versions = ["lts", "1"]
